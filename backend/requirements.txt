fastapi==0.95.1         # FastAPI framework
uvicorn==0.22.0         # ASGI server for running FastAPI (development server)
requests==2.28.1        # To handle HTTP requests to external APIs (e.g., News API)
python-dotenv==0.21.0   # (Optional) If you want to load environment variables from .env file for API keys
